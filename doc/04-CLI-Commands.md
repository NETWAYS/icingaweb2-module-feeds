# CLI Commands

This describes the available CLI commands for this module.

## Cache

The module uses the Icinga Web FileCache to store fetched data.

Available commands:

```bash
# Clears the entire FileCache used by the Feeds module
icingacli feeds cache clear
```
